/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  assetPrefix: '/mission-control-web',
  basePath: '/mission-control-web',
}

module.exports = nextConfig
